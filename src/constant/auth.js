// Created by thanhpd 2/18/2019
// @flow

export default ({
  KEY_CURRENT_PERMISSIONS: 'currentPermissions',
  KEY_CURRENT_USER: 'currentUser',
  api: {
    USER_DETAIL: '/me2',
  },
}: {|
  api: Object,
  KEY_CURRENT_PERMISSIONS: string,
  KEY_CURRENT_USER: string,
|});
